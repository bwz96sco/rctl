# Host compatibility evidence

This disposable release workspace owns one bounded analysis of retained real Codex hook-loading observations. The task under `tasks/hook-loading-compatibility` owns its contract, handoff, result, evidence, and machine acceptance record. The raw host launches are retained evidence. The task contract fixes comparison and budget; the installed rctl CLI owns lifecycle operations.
