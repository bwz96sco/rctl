interface:
  display_name: "Paper Discovery"
  short_description: "Curate deduplicated project paper collections"
  default_prompt: "Use $paper-discovery to find, verify, deduplicate, and add relevant papers to this project's Zotero collection."

policy:
  allow_implicit_invocation: true
